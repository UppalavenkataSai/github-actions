{
  "name": "my-app",
  "version": "1.0.0",
  "scripts": {
    "start": "node src/index.js",
    "lint": "eslint src/",
    "test": "jest",
    "build": "mkdir -p dist && cp -r src/* dist/"
  },
  "devDependencies": {
    "eslint": "^8.50.0",
    "jest": "^29.0.0"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}
